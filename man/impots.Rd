% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateur_impots.R
\name{impots}
\alias{impots}
\title{Estimatore "brut -> net final" (Francia) con IR progressivo e quotient familial}
\usage{
impots(
  brut_annuel,
  cotis_salariales_rate = 0.23,
  net_sur_brut_ratio = NULL,
  abattement10 = TRUE,
  abatt_rate = 0.1,
  abatt_min = 504,
  abatt_max = 14426,
  frais_reels = NULL,
  parts = 1,
  bareme = make_bareme(breaks = c(0, 11497, 29315, 83823, 180294), rates = c(0, 0.11,
    0.3, 0.41, 0.45)),
  hours_week = 35,
  weeks_year = 52
)
}
\arguments{
\item{brut_annuel}{Numeric(1). Salario annuo lordo in euro (>= 0).}

\item{cotis_salariales_rate}{Numeric(1). Tasso stimato delle cotisations salariali
(es. 0.23 => \code{net_avant_impot = brut * (1 - 0.23)}).
Ignorato se \code{net_sur_brut_ratio} è fornito.}

\item{net_sur_brut_ratio}{Numeric(1) oppure NULL. Rapporto netto/lordo
(es. 0.77). Se fornito, sovrascrive \code{cotis_salariales_rate}.}

\item{abattement10}{Logical(1). Se TRUE applica l'abattement 10\% (salarié),
salvo \code{frais_reels} non-NULL.}

\item{abatt_rate}{Numeric(1). Tasso dell'abattement (tipicamente 0.10).}

\item{abatt_min}{Numeric(1). Minimo deducibile (in €) per l'abattement.}

\item{abatt_max}{Numeric(1). Massimo deducibile (in €) per l'abattement.}

\item{frais_reels}{Numeric(1) oppure NULL. Se non-NULL, sostituisce l'abattement:
deduzione fissa in euro (>=0).}

\item{parts}{Numeric(1). Numero di parts per il quotient familial (> 0).}

\item{bareme}{List. Oggetto barème creato con \code{\link[=make_bareme]{make_bareme()}} (breaks + rates).}

\item{hours_week}{Numeric(1). Ore di lavoro settimanali per stimare un netto orario.}

\item{weeks_year}{Numeric(1). Settimane annue per stimare un netto orario.}
}
\value{
\code{data.frame} con una riga e colonne:
\describe{
\item{brut_annuel}{Lordo annuo (input).}
\item{net_avant_impot}{Netto prima dell'IR (stima).}
\item{mode_frais}{Modalità deduzione: "frais_reels", "abattement_10\%", "aucun".}
\item{deduction_frais}{Deduzione applicata (in €).}
\item{revenu_imposable}{Base imponibile stimata dopo deduzioni.}
\item{parts}{Parts utilizzate.}
\item{ir_estime}{IR stimato sul foyer (stima semplificata).}
\item{net_apres_impot}{Netto dopo IR.}
\item{net_mensuel_apres_impot}{Netto mensile dopo IR.}
\item{net_horaire_apres_impot}{Netto orario dopo IR.}
}
}
\description{
Calcola una stima del netto \emph{après impôt} a partire da un salario annuo lordo,
usando:
\itemize{
\item una stima delle cotisations salariali (via \code{cotis_salariales_rate} oppure \code{net_sur_brut_ratio})
\item una base imponibile semplificata (abattement 10\% con min/max oppure frais réels)
\item un calcolo IR progressivo per tranches (barème) con quotient familial (\code{parts})
}
}
\details{
\strong{Nota importante:} stima semplificata. Non include décote, réductions/crédits d'impôt,
prélèvements specifici, esenzioni, né casistiche particolari.
}
\examples{
# Esempio semplice (valori "passe-partout")
brut_to_net_final(
  brut_annuel = 80000,
  cotis_salariales_rate = 0.23,
  parts = 1
)

# Definire un barème esplicito
bareme_2025 <- make_bareme(
  breaks = c(0, 11497, 29315, 83823, 180294),
  rates  = c(0.00, 0.11, 0.30, 0.41, 0.45)
)

brut_to_net_final(
  brut_annuel = 80000,
  cotis_salariales_rate = 0.23,
  parts = 1,
  bareme = bareme_2025,
  hours_week = 35,
  abatt_min = 504,
  abatt_max = 14426
)

}
