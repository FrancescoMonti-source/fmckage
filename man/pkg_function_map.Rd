% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package_introspection.R
\name{pkg_function_map}
\alias{pkg_function_map}
\title{Build per-file and per-function maps from a pkgnet report}
\usage{
pkg_function_map(
  pkg_report,
  r_dir = NULL,
  internal_only = TRUE,
  package = NULL,
  fill_namespace = TRUE
)
}
\arguments{
\item{pkg_report}{Result of \code{pkgnet::CreatePackageReport()}.}

\item{r_dir}{Fallback directory to scan for \code{.R} files (default \code{"R"}).}

\item{internal_only}{If TRUE, keep only edges where both ends are your package's functions.}
}
\value{
A list with \code{by_function} (data.frame), \code{by_file} (data.frame),
and \code{by_file_list} (nested list: file -> function -> list(calls, called_by)).
}
\description{
Produces both views:
\itemize{
\item \code{by_function}: one row per function with its source file, outgoing calls
(\code{calls}) and incoming calls (\code{called_by}) as list-cols, plus counts.
\item \code{by_file}: one row per file with the list of functions and summary counts.
}
}
\details{
Your pkgnet version may not include file paths in nodes; in that case we
statically parse \code{r_dir} to map function -> file.
}
\examples{
\dontrun{
library(pkgnet)
pkg_report <- CreatePackageReport(pkg_name = "yourpkg")
map <- pkg_function_map(pkg_report, r_dir = "R")
map$by_function; map$by_file
}
}
