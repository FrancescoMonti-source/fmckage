% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdescribe.R
\name{fdescribe}
\alias{fdescribe}
\title{fdescribe: Function to compute descriptive statistics}
\usage{
fdescribe(data, vars, group_vars = NULL)
}
\arguments{
\item{data}{A data frame containing the data.}

\item{vars}{A character vector of variable names for which to compute summary statistics.}

\item{group_vars}{A character vector of variable names to group by (optional).}
}
\value{
A data frame with descriptive statistics. If `group_vars` is provided, the output is grouped by these variables. The resulting data frame has statistics as columns and the specified variables as rows.
}
\description{
This function computes descriptive statistics (mean, standard deviation, min, max, range, and standard error) for specified variables in a data frame, optionally grouped by one or more grouping variables.
}
\examples{
# Sample data
data <- tibble(
  PATSEX = rep(c("F", "M"), each = 10),
  PATAGE = rnorm(20, mean = 50, sd = 10),
  taille = rnorm(20, mean = 170, sd = 5)
)

# With grouping variable
result_with_group <- fdescribe(data, vars = c("PATAGE", "taille"), group_vars = "PATSEX")
print(result_with_group)

# Without grouping variable
result_without_group <- fdescribe(data, vars = c("PATAGE", "taille"))
print(result_without_group)

}
