% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateur_impots.R
\name{brut_to_net_final}
\alias{brut_to_net_final}
\title{Estimate French annual net after income tax from annual gross salary (simplified)}
\usage{
brut_to_net_final(
  brut_annuel,
  cotis_salariales_rate = 0.192,
  net_sur_brut_ratio = NULL,
  net_imposable_ratio = 1.03526,
  abattement10 = TRUE,
  abatt_rate = 0.1,
  abatt_min = 504,
  abatt_max = 14426,
  frais_reels = NULL,
  parts = 1,
  bareme = make_bareme(breaks = c(0, 11497, 29315, 83823, 180294), rates = c(0, 0.11,
    0.3, 0.41, 0.45)),
  hours_week = 40,
  weeks_year = 52
)
}
\arguments{
\item{brut_annuel}{Numeric scalar >= 0. Annual gross salary in euros.}

\item{cotis_salariales_rate}{Numeric scalar in [0, 1). Employee contributions rate used
to approximate \code{net_a_payer_avant_pas = brut_annuel * (1 - cotis_salariales_rate)}.
Ignored if \code{net_sur_brut_ratio} is provided.}

\item{net_sur_brut_ratio}{Numeric scalar. If provided, overrides \code{cotis_salariales_rate} and
directly sets \code{net_a_payer_avant_pas = brut_annuel * net_sur_brut_ratio}.}

\item{net_imposable_ratio}{Numeric scalar. Conversion factor from "net à payer (avant PAS)"
to "net imposable". Typically calibrated from a payslip (e.g., \verb{net imposable / net à payer}).}

\item{abattement10}{Logical. If \code{TRUE} and \code{frais_reels} is \code{NULL}, applies the 10\% allowance.}

\item{abatt_rate}{Numeric scalar in [0, 1]. Rate for the 10\% allowance (default 0.10).}

\item{abatt_min}{Numeric scalar >= 0. Minimum allowance in euros.}

\item{abatt_max}{Numeric scalar >= 0. Maximum allowance in euros.}

\item{frais_reels}{Numeric scalar >= 0 or \code{NULL}. If non-\code{NULL}, uses this fixed euro deduction
instead of the 10\% allowance.}

\item{parts}{Numeric scalar > 0. Number of fiscal parts for quotient familial.}

\item{bareme}{List as returned by \code{\link[=make_bareme]{make_bareme()}}. Default is a common recent French schedule.}

\item{hours_week}{Numeric scalar > 0. Weekly working hours used only for the hourly conversion.}

\item{weeks_year}{Numeric scalar > 0. Working weeks per year used only for the hourly conversion.}
}
\value{
A \code{data.frame} with one row and the following columns:
\describe{
\item{cotis_rate_effectif}{Effective contribution rate used.}
\item{mode_frais}{Which deduction mode was applied: \code{"frais_reels"}, \code{"abattement_10\%"}, or \code{"aucun"}.}
\item{brut_annuel}{Input gross annual salary.}
\item{net_a_payer_avant_pas}{Estimated net pay before withholding (approx).}
\item{net_imposable}{Estimated net taxable income used as base for deductions.}
\item{deduction_frais}{Applied deduction amount (allowance or frais réels).}
\item{revenu_imposable}{Taxable income after deductions.}
\item{parts}{Fiscal parts used.}
\item{ir_estime}{Estimated income tax (simplified).}
\item{net_apres_impot}{Estimated net after income tax.}
\item{net_mensuel_apres_impot}{Monthly equivalent.}
\item{net_horaire_apres_impot}{Hourly equivalent based on \code{hours_week * weeks_year}.}
}
}
\description{
Estimates a "net after income tax" from an annual gross salary using:
\enumerate{
\item a social-contributions approximation (\code{cotis_salariales_rate} or \code{net_sur_brut_ratio}),
\item a conversion from "net à payer" to "net imposable" (\code{net_imposable_ratio}),
\item either the 10\% allowance (with min/max) or a fixed \code{frais_reels} deduction,
\item a progressive income tax schedule with quotient familial (\code{parts} and \code{bareme}).
}
}
\details{
This function is designed for \strong{rough budgeting} and scenario comparisons, not
a full pay-slip / tax simulator. It does \strong{not} model the prélèvement à la source
mechanics, decote, credits/reductions, special contributions, caps, or household
specificities beyond \code{parts}.
}
\examples{
# Basic usage with defaults
brut_to_net_final(brut_annuel = 80000, parts = 1)

# Provide a custom barème explicitly
bareme_2025 <- make_bareme(
  breaks = c(0, 11497, 29315, 83823, 180294),
  rates  = c(0.00, 0.11, 0.30, 0.41, 0.45)
)
brut_to_net_final(
  brut_annuel = 70000,
  cotis_salariales_rate = 0.21,
  parts = 1,
  bareme = bareme_2025,
  hours_week = 35,
  abatt_min = 504,
  abatt_max = 14426
)

# Use frais réels instead of the 10\% allowance
brut_to_net_final(
  brut_annuel = 65000,
  parts = 1,
  frais_reels = 2500
)

}
