% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_library.R
\name{audit_library}
\alias{audit_library}
\title{Misura tutti i pacchetti installati}
\usage{
audit_library(packages = NULL, min_functions = 10, quiet = FALSE)
}
\arguments{
\item{packages}{Nomi da misurare. Default: tutti gli installati.}

\item{min_functions}{Scarta i pacchetti sotto questa soglia: sotto le ~10
funzioni le percentuali sono rumore.}

\item{quiet}{Se \code{FALSE}, stampa l'avanzamento (l'operazione dura minuti).}
}
\value{
Data frame, una riga per pacchetto, con le stesse colonne di
\code{audit_repo(...)$summary} piu' \code{version} e \code{priority}.
}
\description{
Costruisce la distribuzione di riferimento contro cui leggere l'audit di un
repo. I pacchetti installati non contengono i sorgenti, quindi la misura
passa dal namespace (si veda \code{\link[=fn_objects_in_namespace]{fn_objects_in_namespace()}}).
}
\section{Effetti collaterali}{

Misurare un pacchetto richiede \code{loadNamespace()}, che esegue il suo
\code{.onLoad()} -- lo stesso codice che gira a ogni \code{library()}. Sono pacchetti
gia' installati sulla macchina. I pacchetti che non si caricano vengono
saltati e riportati in \code{attr(x, "failed")}.
}

\examples{
\dontrun{
base <- audit_library()
saveRDS(base, "~/baseline_cran.rds")
}
}
\seealso{
\code{\link[=audit_repo]{audit_repo()}}, \code{\link[=compare_to_library]{compare_to_library()}}
}
