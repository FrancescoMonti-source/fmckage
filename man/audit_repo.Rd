% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_repo.R
\name{audit_repo}
\alias{audit_repo}
\alias{print.repo_audit}
\title{Audit strutturale di un repo R}
\usage{
audit_repo(path, dirs = NULL, exclude = c("tools"), recursive = FALSE)

\method{print}{repo_audit}(x, n = 20, ...)
}
\arguments{
\item{path}{Radice del repo.}

\item{dirs}{Cartelle da misurare, relative a \code{path}. Default \code{NULL}: usa
\verb{R/} se esiste, altrimenti la radice. \verb{scripts/} non e' incluso di
default -- gli script d'ingresso hanno una forma diversa dal codice di
libreria e mescolarli falsa il confronto con \code{\link[=audit_library]{audit_library()}}. Per
guardarli si passi esplicitamente \code{dirs = c("R", "scripts")}.}

\item{exclude}{Cartelle da escludere dal conteggio (default: \code{"tools"}, di
solito script usa-e-getta che sporcano le funzioni senza chiamanti).}

\item{recursive}{Se \code{TRUE}, scende nelle sottocartelle di \code{dirs}.}

\item{x}{Oggetto \code{repo_audit}.}

\item{n}{Righe da mostrare per sezione.}

\item{...}{Ignorato.}
}
\value{
Oggetto di classe \code{repo_audit}: lista con \code{functions} (una riga per
funzione), \code{by_file}, \code{summary} (una riga di metriche confrontabili),
\code{patterns} e i metadati del repo. Stampalo per il report leggibile.
}
\description{
Misura ogni funzione definita nel repo e restituisce le tabelle su cui
impostare una revisione: complessita' ciclomatica, numero di chiamanti,
uso nei test, densita' di guardie e controlli.
}
\details{
Le metriche sono le stesse di \code{\link[=audit_library]{audit_library()}}, cosi' il risultato si puo'
collocare in una distribuzione di riferimento con \code{\link[=compare_to_library]{compare_to_library()}}.
}
\section{Cosa non vede}{

Il grafo delle chiamate e' statico: dispatch S3/S4, \code{do.call()} e \code{get()}
non sono rilevati, quindi \code{callers == 0} va letto come "da verificare", non
come "morto". Le funzioni annidate dentro altre funzioni non sono contate.
}

\examples{
\dontrun{
a <- audit_repo("C:/Users/franc/Documents/Git/redsan")
a
subset(a$functions, callers == 1 & cc > 10)

# includendo anche gli script d'ingresso
audit_repo("C:/Users/franc/Documents/Git/orchidee", dirs = c("R", "scripts"))
}
}
\seealso{
\code{\link[=audit_library]{audit_library()}}, \code{\link[=compare_to_library]{compare_to_library()}}
}
