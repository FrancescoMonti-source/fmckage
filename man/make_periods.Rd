% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_periods.R
\name{make_periods}
\alias{make_periods}
\title{Generate sequential date periods between two dates}
\usage{
make_periods(
  start_date,
  end_date,
  sep = ",",
  by = "6 months",
  prefix = "",
  suffix = ""
)
}
\arguments{
\item{start_date}{Date or character coercible to Date. Range start (inclusive).}

\item{end_date}{Date or character coercible to Date. Range end (inclusive).}

\item{sep}{String between start and end in each period label. Default \code{","}.}

\item{by}{Interval passed to \code{\link[=seq.Date]{seq.Date()}} (e.g. \code{"1 month"}, \code{"3 months"},
\code{"30 days"}, \code{"2 weeks"}). Default \code{"6 months"}.}

\item{prefix}{String prepended to each label. Default \code{""}.}

\item{suffix}{String appended to each label. Default \code{""}.}
}
\value{
A tibble with columns:
\describe{
\item{start}{Start date of the period (inclusive).}
\item{end}{End date of the period (inclusive).}
\item{period}{Character label: \code{prefix + start + sep + end + suffix}.}
}
}
\description{
Creates a tibble of contiguous periods covering \link{start_date, end_date}.
The length of each period is controlled by \code{by} (passed to \code{\link[=seq.Date]{seq.Date()}}),
and labels are built with optional \code{prefix}, \code{suffix}, and \code{sep}.
}
\details{
Periods are generated by \code{starts <- seq(start_date, end_date, by = by)}.
Each period's end is the day before the next period's start; the final
period ends at \code{end_date}. No alignment to calendar boundaries is applied.
}
\examples{
make_periods("2012-01-01", "2012-07-31", by = "3 months",
             prefix = "{", suffix = "}")
# -> 2012-01-01..2012-03-31, 2012-04-01..2012-06-30, 2012-07-01..2012-07-31

}
