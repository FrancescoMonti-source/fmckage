% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_library.R
\name{compare_to_library}
\alias{compare_to_library}
\title{Colloca un repo nella distribuzione dei pacchetti installati}
\usage{
compare_to_library(
  x,
  baseline = library_baseline(),
  contributed_only = TRUE,
  metrics = NULL
)
}
\arguments{
\item{x}{Oggetto \code{repo_audit} da \code{\link[=audit_repo]{audit_repo()}}, o il suo \verb{$summary}.}

\item{baseline}{Data frame da \code{\link[=audit_library]{audit_library()}}. Default: la copia salvata
su questa macchina, costruita alla prima chiamata.}

\item{contributed_only}{Se \code{TRUE}, esclude i pacchetti base e recommended,
scritti in un'epoca e con vincoli diversi.}

\item{metrics}{Metriche da confrontare. Default: tutte quelle comuni.}
}
\value{
Data frame con \code{metric}, \code{value}, \code{percentile}, \code{baseline_median}.
}
\description{
Per ogni metrica restituisce il valore del repo, il percentile che occupa
nella base di confronto e la mediana di riferimento.
}
\section{Come leggerlo}{

I pacchetti su CRAN \emph{sono} librerie per un pubblico ampio: sulla superficie
pubblica e sulla difensivita' sono il termine di paragone sbagliato per del
codice interno, e uno scarto verso il basso li' non e' un difetto. Sulla
complessita' per funzione e sull'organizzazione interna il confronto regge.
}

\examples{
\dontrun{
base <- audit_library()
compare_to_library(audit_repo("~/Git/redsan"), base)
}
}
