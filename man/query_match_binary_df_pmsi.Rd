% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_match_binary_df_pmsi.R
\name{query_match_binary_df_pmsi}
\alias{query_match_binary_df_pmsi}
\title{Match multiple queries against EDSaN 'doceds' using d2imr and return binary indicators}
\usage{
query_match_binary_df_pmsi(
  query_list = NULL,
  PATID = "",
  EVTID = "",
  ELTID = "",
  PATBD = "",
  PATAGE = "",
  PATSEX = "",
  SEJUM = "",
  SEJUF = "",
  DATENT = "",
  DATSORT = "",
  SEJDUR = "",
  MODEENT = "",
  MODESORT = "",
  PMSISTATUT = "",
  CODEACTE = "",
  DATEACTE = "",
  NOMENCLATURE = "",
  UFPRO = "",
  UFDEM = ""
)
}
\arguments{
\item{query_list}{A named list of query strings. Each name will become a binary column in the output.}

\item{PATID}{Optional filter for patient ID. Defaults to "".}

\item{EVTID}{Optional filter for event ID. Defaults to "".}

\item{PATBD}{Optional filter for birth date (e.g., ">2000"). Defaults to "".}

\item{PATAGE}{Optional filter for patient age. Defaults to "".}

\item{PATSEX}{Optional filter for patient sex. Defaults to "".}

\item{SEJUM}{Optional filter for medical unit. Defaults to "".}

\item{SEJUF}{Optional filter for medical service. Defaults to "".}

\item{DATENT}{Optional filter for day of hospitalisation. The scope is a single RUM not the entire hospital stay. Defaults to "".}

\item{DATSORT}{Optional filter for descharge day. The scope is a single RUM not the entire hospital stay. Defaults to "".}

\item{CODEACTE}{Optional filter CCAM acts. Defaults to "".}

\item{DATEACTE}{Optional filter CCAM acts date. Defaults to "".}

\item{NOMENCLATURE}{Optional filter for the nomenclature the specified act belongs to. Defaults to "".}

\item{UFPRO}{Optional filter for the service who realised the CCAM act. Defaults to "".}

\item{UFDEM}{Optional filter for the service who prescribed the CCAM act to be realised. Defaults to "".}

\item{DALL}{Optional filter for the service who realised the CCAM act. Defaults to "".}
}
\value{
A data frame with (PATID, EVTID) and binary columns for each query. Absence of a match is left encoded as NA.
}
\description{
This function queries the EDSaN document database (module 'doceds') via the \code{d2imr} API,
applying multiple keyword searches defined in \code{query_list}. It returns a wide-format data frame
with one row per unique (PATID, EVTID) combination and one binary column per query, indicating
whether a match was found.
}
\examples{
query_list <- list(
  rachitisme = "E55*",
  osteoporose = "M80* OR M81*"
)
query_match_binary_df_doceds(query_list, PATID = "123456", PATBD = ">2000", RECDATE = "<2025")

}
