% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_idtriplets.R
\name{count_idtriplets}
\alias{count_idtriplets}
\title{Count Unique ID Triplets}
\usage{
count_idtriplets(
  data,
  cols = c("patid", "evtid", "eltid", "doc_id", "pmsi_id")
)
}
\arguments{
\item{data}{A dataframe containing the data to analyze.}

\item{cols}{A character vector of column names to count distinct values from. Default is \code{c("patid", "evtid", "eltid", "doc_id", "pmsi_id")}.
If not provided, it attempts to find these columns in the data (case-insensitive).}
}
\value{
This function prints the distinct counts for the specified columns in the dataset.
}
\description{
This function counts distinct values for specific columns in a dataset, which typically represent patient IDs, event IDs, or other identifier columns.
The function is case-insensitive and allows you to specify the columns to count.
}
\examples{
# Example usage:
data <- data.frame(
  patid = c(1, 1, 2, 3),
  evtid = c(101, 102, 101, 103),
  eltid = c(201, 202, 201, 203),
  doc_id = c(301, 302, 301, 303),
  pmsi_id = c(401, 402, 401, 403)
)
count_idtriplets(data)
}
